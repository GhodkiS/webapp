pipeline {
    agent any
    stages {
        stage('Test') {
            steps {
                sh 'pwd'
                sh 'docker ps -a'
                sh 'docker images'
               
            }
        }
    }
}
