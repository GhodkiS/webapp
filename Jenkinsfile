pipeline {
    agent any
    stages {
        stage('Test') {
            steps {
                
                sh 'echo "this is the text" > testfile'
              
             }
           
            
        }
    }
}
